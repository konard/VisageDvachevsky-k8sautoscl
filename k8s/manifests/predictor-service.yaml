apiVersion: v1
kind: Service
metadata:
  name: predictor
  namespace: predictive-autoscaling
  labels:
    app: predictor
    component: inference
spec:
  selector:
    app: predictor
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
